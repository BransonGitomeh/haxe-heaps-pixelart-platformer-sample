{
    "scripts": {
        "dev":"export PORT=3000 && npx livereload build -p $PORT",
        "start":"npx http-server build -p $PORT",
        "postinstall": "haxelib install all && haxe build.hxml"
    }
}